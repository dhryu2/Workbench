# ============================================================
#  OS / editor
# ============================================================
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/
*.iml

# ============================================================
#  OMC operational state
# ============================================================
.omc/

# ============================================================
#  Frontend — Node / Vite / React / TypeScript
# ============================================================
node_modules/
dist/
dist-ssr/
build/
coverage/
*.local

# env files (keep .env.example if you add one)
.env
.env.*
!.env.example

# ============================================================
#  Deployment — secrets & credentials (never commit)
# ============================================================
# 운영 시크릿(.env.production 등)은 위 .env.* 로 이미 무시되지만, 아래를 명시해 실수 커밋 방지
.env.production
.env.production.*
# SSH 개인키 / 인증서 / 자격증명
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
id_ecdsa
*_rsa
*_ed25519
authorized_keys
known_hosts
# 배포 산출물 / 원격에 전송되는 임시 compose 렌더링
docker-compose.override.yml
*.tar
*.tar.gz

# logs & debug
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# tooling caches
.vite/
.eslintcache
*.tsbuildinfo

# ============================================================
#  Backend — Java / Gradle / Spring Boot
# ============================================================
.gradle/
**/build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/**/build/

# compiled output / IDE
bin/
out/
*.class

# Spring / app runtime
*.pid

# keep the Gradle wrapper
!gradlew
!gradlew.bat

# Claude Code instructions (local only)
CLAUDE.md
docs
